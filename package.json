{
  "name": "opencode-git-add",
  "version": "0.1.0",
  "description": "opencode plugin: run `git add .` after each finished turn, only in jujutsu colocated repos (.git + .jj both present)",
  "main": "git-add.ts",
  "module": "git-add.ts",
  "files": [
    "git-add.ts"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "1.17.8",
    "typescript": "^5"
  },
  "license": "MIT"
}