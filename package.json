{
  "name": "opencode-git-add",
  "version": "0.2.0",
  "description": "opencode plugin: run `git add .` after each finished turn in git repositories, so the unstaged diff view of the current turn is always reviewable",
  "main": "git-add.ts",
  "module": "git-add.ts",
  "files": [
    "git-add.ts"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "1.17.8",
    "typescript": "^5"
  },
  "license": "MIT"
}