{
  "name": "opencode-git-add",
  "version": "0.6.0",
  "description": "opencode plugin: git add . at the start of each main-session turn (subagent turns skipped), so the unstaged diff always shows only the in-progress turn's changes",
  "main": "git-add.ts",
  "module": "git-add.ts",
  "files": [
    "git-add.ts"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "1.17.8",
    "@types/node": "^26.4.0",
    "typescript": "^5"
  },
  "license": "MIT"
}